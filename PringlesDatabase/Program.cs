using System;
using System.ComponentModel;
using System.ComponentModel.DataAnnotations;
using System.Linq;
using PringlesDatabase.Models;

namespace PringlesDatabase
{
    class Program
    {
        static void Main(string[] args)
        {
            /*using (var db = new PringlesContext())
            {
                var user = db.Users.Where(x=>x.Username == "User1").ToList();
                var rating = db.Ratings.Where(x=>x.User.Username == "User1").ToList();
                var pringles = db.Pringleses.ToList();
                Console.WriteLine();

            }*/
        }
    }
}
